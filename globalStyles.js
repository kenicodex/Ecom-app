import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    header: {
        // height:30,
        paddingVertical: 10,
        backgroundColor: 'white',
        position: 'relative',
        zIndex: 10
    },
})